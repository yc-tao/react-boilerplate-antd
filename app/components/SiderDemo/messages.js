/*
 * SiderDemo Messages
 *
 * This contains all the text for the SiderDemo component.
 */

import { defineMessages } from 'react-intl';

export const scope = 'app.components.SiderDemo';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'This is the SiderDemo component!',
  },
});
